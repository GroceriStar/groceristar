nav.navbar.navbar-default.navbar-fixed-top(role='navigation')
  .container
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') STARTAPE NAME
    #navbar.navbar-collapse.collapse
      if user
        //- User *is* logged In
        ul.nav.navbar-nav.navbar-right

          li(class=url=='/auth/account'?'active':undefined)
            a(href='/auth/account') View Account

          //if( url== '/view/grocery/')  
          li
            a= url
          li
            a(href='/auth/account') Clone Grocery List  
          li
            a(href='/auth/logout') Log Out
      else
        //- User is *not* Logged In
        ul.nav.navbar-nav.navbar-right
          li(class=url=='/'?'active':undefined)
            a(href='/') Home
          li
            a(href='/auth/facebook') Login with Facebook

          //li(class=url=='/login'?'active':undefined)
            //a(href='/login') Log in
          //li(class=url=='/signup'?'active':undefined)
            //a(href='/signup') Sign Up
