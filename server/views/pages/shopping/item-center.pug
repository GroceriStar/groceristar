label.list__item__center(for="checkbox" + element.name) 
    
  if( element.completed )
    // change it :todo
    span.completka=element.name
  else	    	
    span=element.name

  // :todo change it      
  input.text-input.edit(type="text", value=element.name, style="display: none;")